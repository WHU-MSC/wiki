# WHUMSC 2026 秋季招新笔试题

你好，这里是计算机学院 2024 级软件工程专业本科生黄霆瑄，新任的~~被上任社长忽悠来的~~ WHUMSC 社长，非常欢迎各位新生（和老登）加入我们的社团！按照本社团惯例，我们的招新设置笔试和面试，主要目的是了解新社员的实际情况，以作为后续开展活动的参考，所以各位刚刚过完高三的新生们不要 PTSD，按自己的真实想法作答即可 : D

作答方式不限，手写拍照、Word 文档都可以。

那么，我们开始吧？ 

## Part 1. 调研题

1. 做一个简单的自我介绍吧！
2. 你是在哪里看到我们的招新信息的？
3. WHUMSC 为什么会吸引你加入进来？
4. 你希望在这里参加什么样的活动？

## Part 2. 个人经历题

1. 你以前参加过任何种类的面试吗？
   1. 如果有，你当时感觉如何？
   2. 如果没有，你认为面试会是什么样的？
2. 可以分享一件你干过的最技术、最“硬核”的事吗？不一定需要与计算机相关哦。
3. 你目前在使用哪些 AI 工具？感受如何？

## Part 3. 技术题

Tips：你可以借助任何搜索引擎或任意 AI 工具完成本部分题目。

1. 推测一下这套面试题是用什么写的 : D
2. 观察下面的定义，尝试进行推演运算，并推测 `Y` 的性质：

```
g(x) = x(x)
W(f)(x) = f(g(x))
Y(f) = W(f)(W(f))
```

针对任意形如 `f(...) = ... f(...) ...` 的递归函数，你可以通过使用 `Y` 将其重新构造成一个**不直接进行递归**的函数吗？

### 提示材料

* 柯里化：任何函数都是一元函数。`add(x, y)` 可以被写做 `add(x)(y) = x + y`。你可以进行“部分应用”，如：`add(1)(y) = 1 + y`；注意你此时其实创造了一个新的函数，直观上这个函数或许可以被叫作……`add(1)`？

* 递归总可以写成 `f = g(f)`。你当然可以不把它当成定义，而当成是一个等式、一条性质。`f` 和 `g` 的关系是？想想 `Y` 呢？
